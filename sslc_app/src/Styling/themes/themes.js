import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    typography: {
        body1: {
            fontSize: '1.2rem',
        },
        h1: {
            fontSize: '2.2rem',
        },
    },
});

export default theme;